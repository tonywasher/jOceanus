/* Dependencies */
dependencies {
    implementation project(':jtethys-core')
}

/* Description */
description = 'Prometheus Sheet Service API'

/* Register the project */
registerProject(project) 

/* Specific Manifest details */
jar {
    manifest {
        attributes(manifestAttrs)
    }
}
