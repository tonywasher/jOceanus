<document>
    <properties>
        <title>GordianKnot SecureRandom</title>
        <author email="Tony.Washer@yahoo.co.uk">Tony Washer</author>
    </properties>
    <body>
        <section name="GordianKnot SecureRandom">
            <subsection name="Overview">
                <p>SecureRandoms are supported via the <strong>GordianRandomFactory</strong> interface.</p>
                <p><strong>GordianKnot</strong> supports the <strong>SP800-90A</strong> and <strong>X931</strong> DRBG constructs.</p>
                <p><strong>GordianKnot</strong> also supports a combined secureRandom wher the output of a CTR SP800 instance
                    is combined with a HASH SP800 instance with the generated byte streams being xor-ed with each other.
                    In this case HASH is restricted to those digests with an output length of 512 bits, and CTR is restricted
                    to those symKeys with length and Block size of 128 bits.</p>
                <p>Internally <strong>GordianKnot</strong> uses a random combined secureRandom.</p>
            </subsection>
            <subsection name="Randoms">
                <p>A secureRandom is created by specifying a <strong>GordianRandomSpec</strong>.</p>
                <subsection name="Sample">
                    <source>
/* Access factory */
final GordianFactory myBaseFactory = GordianGenerator.createFactory();
final GordianRandomFactory myRandomFactory = myBaseFactory.getRandomFactory();

/* Create secureRandom */
final GordianRandomSpec myHashSpec = GordianRandomSpecBuilder.hash(GordianDigestSpec.sha2(GordianLength.LEN_512));
final SecureRandom myRandom = myRandomFactory.createRandom(myHashSpec);

/* Create combinedRandom */
final GordianRandomSpec myCtrSpec = GordianRandomSpecBuilder.ctr(GordianSymKeySpec.aes2(GordianLength.LEN_128));
final SecureRandom myComboRandom = myRandomFactory.createRandom(myCtrSpec, myHashSpec);
                    </source>
                </subsection>
                <subsection name="Algorithms">
                    <p>The following algorithms are supported.</p>
                    <table class="defTable">
                        <tr><th class="defHdr">Algorithm</th></tr>
                        <tr><td>SP800-HASH</td></tr>
                        <tr><td>SP800-HMAC</td></tr>
                        <tr><td>SP800-CTR</td></tr>
                        <tr><td>X9.31</td></tr>
                    </table>
                </subsection>
            </subsection>
        </section>
    </body>
</document>
