#Build Details.

name=${project.name}
groupId=${project.groupId}
artifactId=${project.artifactId}
version=${project.version}
revision=${buildNumber}
dependencies=JDataModel,JDataManager,JGordianKnot,JDecimal,JDateDay,JHelpManager,JSortedList,ApachePOI
